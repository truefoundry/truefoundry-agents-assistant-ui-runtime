node_modules/
dist/
.next/
.env*
!.env.example
*.pem
*.tsbuildinfo
.DS_Store

# CodeGraph (local index, daemon, cache — per-machine)
.codegraph/

# npm lock file (this repo uses pnpm)
package-lock.json
